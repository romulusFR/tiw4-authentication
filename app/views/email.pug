extends layout

block content

    .main.container
        .row
            .col-md-6.col-md-offset-3
                h2.display-6.m-b-2 Reset password email simulation

                form(method='GET' action='/reset')
                    p to reset password please click the button
                    input#token.form-control(type='hidden', value=tk name='token')
                    button.btn.btn-primary(type='submit') Reset Password
